FROM node:24-alpine

WORKDIR /app

# Dependências para módulos nativos (ex.: engines do Prisma)
RUN apk add --no-cache python3 make g++ openssl

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 3000

ENV NODE_ENV=development

CMD ["npm", "run", "start:dev"]
